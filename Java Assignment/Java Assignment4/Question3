/*
3. Create a class named 'Rectangle' with two data members 'length' and 'breadth' and
two methods to print the area and perimeter of the rectangle respectively. Its
constructor having parameters for length and breadth is used to initialize length and
breadth of the rectangle. Let class 'Square' inherit the 'Rectangle' class with its
constructor having a parameter for its side. Print the area and perimeter of a rectangle
and a square.*/

import java.util.Scanner; 
public class Methods {
	public static void main(String[] args) {
		
			Rectangles r1=new Rectangles(5,5); 
			r1.printarea();
			r1.printperimeter(); 
			Square s=new Square(5);; 
			s.printarea();
			s.printperimeter(); 
		
		
		}
	}
public  class Rectangles {

	final int length; 
	int breadth; 
	Rectangles(int l,int b){
		length=l; 
		breadth=b; 
	}
	
	 void printarea() {
		System.out.println("Area of Rectangles is : "+(length*breadth));
	}
     void printperimeter() {
		System.out.println("Perimeter of Rectagnel is :"+(2*(length*breadth)));
	}
}

public class Square extends Rectangles {

		int size; 
		Square(int size){
			super(size,size);
		}
		
		void printarea() {
			System.out.println("how areyoeu "); 
		}
}
